  <tr id=<%="ofrendanual_#{ofrendanual.id}"%>>
      <tr>
        <td><%= link_to ofrendanual.id, ofrendanual_path(ofrendanual) %></td>
        <td><%= ofrendanual.valor %></td>
        <td><%= ofrendanual.año %></td>
        <td><%=l ofrendanual.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_ofrendanual_path(ofrendanual), remote: true, :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      ofrendanual_path(ofrendanual),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    