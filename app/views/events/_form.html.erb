<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br>
    <%= f.text_field :title %>
  </div>
<div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :Dia_De_Comienzo %><br>
    <%= f.datetime_select :start %>
  </div>
  <div class="field">
    <%= f.label :Dia_De_Fin %><br>
    <%= f.datetime_select :end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
