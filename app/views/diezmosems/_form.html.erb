<%= simple_form_for @diezmosem, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :valor %>
  <%= f.input :semana %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                diezmosems_path, :class => 'btn' %>
  </div>
<% end %>
